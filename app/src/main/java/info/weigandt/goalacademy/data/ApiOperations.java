package info.weigandt.goalacademy.data;

public class ApiOperations {

    /**
     * Start the async task using Retrofit.
     * "this" is passed as listener to the controller, so that MainActivity
     *  can be called when request is finished
     */

    /*
    private void performTheMovieDbListLoading(@NonNull Context context) {
        MainActivity mainActivity = context;
        /*
        if (mIsRestoredFromState == false)
        {
            showMainLoadingIndicator();
        }

        mainActivity.mQuotesController = new QuotesController(this);

        switch (mode){
            case NetworkHelper.MODE_POPULAR:
                mMoviesController.startLoadingPopularMovies();
                break;
            case NetworkHelper.MODE_TOP_RATED:
                mMoviesController.startLoadingTopRatedMovies();
                break;
        }
        */

}
